---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';

// --- DADOS GLOBAIS ---
const cidade = "Divinésia";
const estado = "MG";
const whatsapp = "553298408646";
const telefone = "(32) 9 9840-8646";
const telefoneLink = "553298408646";

// --- DADOS CORRIGIDOS E PREENCHIDOS ---
const emailContato = "ramundo@divcomp.com";
const formsubmitAction = `https://formsubmit.co/${emailContato}`;
const instagramLink = "https://www.instagram.com/divcomp";
const facebookLink = "https://www.facebook.com/divcomp";
// ATENÇÃO: Preencha este link com o ID do seu Perfil da Empresa no Google.
const urlAvaliarGoogle = "https://g.page/r/YOUR_PLACE_ID/review";

const avaliacoes = [
    {
        nome: "Maria Korsgaard",
        foto: "/images/cliente1.jpg", // ✅ Você precisa adicionar esta imagem na pasta public/images
        data: "2023-03-15",
        texto: "O atendimento foi impecável! Recomendo demais, resolveram meu problema e deram total atenção.",
        estrelas: 5
    },
    {
        nome: "Maren Calzoni",
        foto: "/images/cliente2.jpg", // ✅ Você precisa adicionar esta imagem na pasta public/images
        data: "2023-03-12",
        texto: "Serviço rápido, muito profissional. Explicou tudo com paciência. Empresa confiável.",
        estrelas: 5
    },
    {
        nome: "Davis Dokidis",
        foto: "/images/cliente3.jpg", // ✅ Você precisa adicionar esta imagem na pasta public/images
        data: "2023-03-08",
        texto: "Fiz curso e também usei o suporte. Nota máxima. Recomendo a todos da região!",
        estrelas: 5
    },
    {
        nome: "Ana Silva",
        foto: "/images/cliente4.jpg", // ✅ Você precisa adicionar esta imagem na pasta public/images
        data: "2024-01-20",
        texto: "Profissionalismo e conhecimento técnico definem o serviço. A consultoria em redes foi essencial para minha empresa.",
        estrelas: 5
    },
];
---

<html lang="pt-BR" class="scroll-smooth">
<head>
    <BaseHead
            title="DivComp Soluções | Especialista em TI, Cursos e Manutenção"
            description="Leadpage oficial da DivComp Soluções: manutenção de computadores, TI, cursos, recuperação de dados e avaliações reais. Atendimento em Divinésia/MG e região."
    />
    <style is:inline>
        .hide-scrollbar::-webkit-scrollbar {
            display: none;
        }
        .hide-scrollbar {
            -ms-overflow-style: none;  /* IE and Edge */
            scrollbar-width: none;  /* Firefox */
        }
    </style>
</head>
<body class="bg-white text-gray-900 font-sans antialiased">
<Header/>

<main>
    {/* Seções Hero, Contato Rápido e Serviços (sem alterações) */}
    <section class="relative w-full bg-white py-20 md:py-28 scroll-mt-24">
        <div class="absolute inset-0 -z-10 overflow-hidden">
            <svg class="absolute left-[50%] top-0 h-[100%] w-[100%] -translate-x-1/2" viewBox="0 0 1024 1024" aria-hidden="true">
                <circle cx="512" cy="512" r="512" fill="url(#gradient-radial-hero)" fill-opacity="0.2"></circle>
                <defs>
                    <radialGradient id="gradient-radial-hero">
                        <stop stop-color="#4ade80"></stop>
                        <stop offset="1" stop-color="#065f46"></stop>
                    </radialGradient>
                </defs>
            </svg>
        </div>
        <div class="max-w-4xl mx-auto flex flex-col items-center text-center gap-6 px-6">
			<span class="block text-base md:text-lg font-semibold text-green-700">
				Suporte Técnico e Treinamentos
			</span>
            <h1 class="text-5xl sm:text-6xl lg:text-7xl font-extrabold tracking-tight text-balance bg-gradient-to-r from-green-600 to-emerald-800 bg-clip-text text-transparent">
                +20 Anos de Experiência Real em TI
            </h1>
            <p class="text-lg md:text-xl text-gray-700 mt-2 max-w-2xl">
                Soluções definitivas para seu computador, rede e tecnologia. Fale com quem realmente entende do assunto e resolve seu problema.
            </p>
        </div>
    </section>
    <section class="relative z-10">
        <div class="max-w-4xl mx-auto px-6 -mt-16">
            <div class="bg-white rounded-2xl shadow-xl border border-green-100/80 p-6 md:p-8">
                <div class="grid grid-cols-1 md:grid-cols-12 items-center gap-6 md:gap-4">
                    <div class="md:col-span-2 flex justify-center">
                        <span class="icon-[mdi--face-agent] text-7xl text-green-600"></span>
                    </div>
                    <div class="md:col-span-6 text-center md:text-left">
                        <h3 class="text-xl md:text-2xl font-bold text-gray-800">Precisa de uma solução rápida?</h3>
                        <p class="text-gray-600 mt-1">Sem robôs ou menus complicados. Fale diretamente com um especialista.</p>
                    </div>
                    <div class="md:col-span-4 flex justify-center md:justify-end">
                        <a href={`https://wa.me/${whatsapp}`} target="_blank" rel="noopener" class="inline-flex items-center gap-3 bg-green-600 hover:bg-green-700 text-white font-bold px-6 py-3 rounded-lg shadow-lg transition-transform hover:scale-105 w-full md:w-auto justify-center">
                            <span class="icon-[mdi--whatsapp] w-6 h-6"></span>
                            <span>{telefone}</span>
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </section>
    <section id="servicos" class="w-full py-16 md:py-24 bg-slate-50 border-b border-green-100">
        <div class="max-w-5xl mx-auto px-6 md:px-10">
            <h2 class="text-center text-base/7 font-semibold text-green-600">Nossos Serviços</h2>
            <p class="mx-auto mt-2 max-w-2xl text-center text-4xl font-semibold tracking-tight text-balance text-gray-900 sm:text-5xl">
                Soluções completas para suas necessidades de tecnologia
            </p>
            <div class="mt-12 grid grid-cols-1 md:grid-cols-3 gap-8">
                <div class="bg-white rounded-xl shadow p-8 flex flex-col items-center gap-3 border border-green-100 text-center">
                    <span class="icon-[mdi--monitor] text-4xl text-green-600 mb-3"></span>
                    <h3 class="text-xl font-semibold text-gray-900">Manutenção de Computadores</h3>
                    <p class="text-gray-700">Formatar, limpar, otimizar, instalar e recuperar dados de notebook e PC.</p>
                </div>
                <div class="bg-white rounded-xl shadow p-8 flex flex-col items-center gap-3 border border-green-100 text-center">
                    <span class="icon-[mdi--server] text-4xl text-green-700 mb-3"></span>
                    <h3 class="text-xl font-semibold text-gray-900">TI Empresarial</h3>
                    <p class="text-gray-700">Redes, servidores, backup, internet, impressoras e suporte para seu negócio.</p>
                </div>
                <div class="bg-white rounded-xl shadow p-8 flex flex-col items-center gap-3 border border-green-100 text-center">
                    <span class="icon-[ph--chalkboard-teacher-bold] text-4xl text-green-700 mb-3"></span>
                    <h3 class="text-xl font-semibold text-gray-900">Cursos e Aulas</h3>
                    <p class="text-gray-700">Aulas de informática, Office e tecnologia. Capacitação para todas as idades.</p>
                </div>
            </div>
        </div>
    </section>

    <section id="avaliacoes-google" class="w-full py-16 md:py-24 bg-white border-b border-green-100 overflow-hidden">
        <div class="max-w-6xl mx-auto px-4 flex flex-col items-center">
            <h2 class="text-center text-base/7 font-semibold text-green-600">Depoimentos</h2>
            <p class="mx-auto mt-2 max-w-2xl text-center text-4xl font-semibold tracking-tight text-balance text-gray-900 sm:text-5xl">
                O que nossos clientes dizem sobre nós
            </p>

            <div x-data="{
					atBeginning: true,
					atEnd: false,
					scroll(direction) {
						const slider = this.$refs.slider;
						const slideWidth = slider.querySelector('div').clientWidth;
						slider.scrollBy({ left: slideWidth * direction, behavior: 'smooth' });
					},
					updateArrows() {
						const slider = this.$refs.slider;
						this.atBeginning = slider.scrollLeft <= 0;
						this.atEnd = Math.abs(slider.scrollWidth - slider.clientWidth - slider.scrollLeft) < 5;
					}
				}"
                 x-init="updateArrows()"
                 class="w-full relative mt-12"
            >
                <div class="absolute inset-0 flex items-center justify-between z-10 pointer-events-none">
                    <button @click="scroll(-1)" :disabled="atBeginning" class="pointer-events-auto bg-white/80 hover:bg-white text-green-700 font-bold p-3 rounded-full shadow-lg transition disabled:opacity-50 disabled:cursor-not-allowed -ml-4 md:-ml-6" aria-label="Avaliação anterior">
                        <span class="icon-[mdi--arrow-left] w-6 h-6"></span>
                    </button>
                    <button @click="scroll(1)" :disabled="atEnd" class="pointer-events-auto bg-white/80 hover:bg-white text-green-700 font-bold p-3 rounded-full shadow-lg transition disabled:opacity-50 disabled:cursor-not-allowed -mr-4 md:-mr-6" aria-label="Próxima avaliação">
                        <span class="icon-[mdi--arrow-right] w-6 h-6"></span>
                    </button>
                </div>

                <div x-ref="slider" @scroll.debounce.150ms="updateArrows()" class="flex overflow-x-auto snap-x snap-mandatory scroll-smooth hide-scrollbar -mx-4 px-4">
                    {avaliacoes.map((a) => (
                            <div class="flex-shrink-0 w-full md:w-1/2 lg:w-1/3 snap-center px-2">
                                <div class="bg-white border border-gray-200 rounded-2xl shadow-md flex flex-col text-center p-8 h-full">
                                    <img src={a.foto} alt={`Foto de ${a.nome}`} class="w-20 h-20 rounded-full object-cover border-2 border-green-200 mb-4 mx-auto" loading="lazy"/>
                                    <span class="font-semibold text-gray-900 text-lg">{a.nome}</span>
                                    <span class="text-sm text-gray-500 mb-2">{new Date(a.data + 'T00:00:00').toLocaleDateString('pt-BR')}</span>
                                    <div class="flex justify-center gap-0.5 mb-3">
                                        {[...Array(a.estrelas)].map(() => <span class="icon-[mdi--star] text-yellow-400 w-5 h-5"></span>)}
                                    </div>
                                    <p class="text-gray-700 text-base mt-1 mb-4 flex-grow">"{a.texto}"</p>
                                    <span class="flex items-center gap-1.5 justify-center mt-auto pt-4 border-t border-gray-100">
									<img src="/images/google-verified-reviews.png" alt="Logo do Google Verified Reviews" class="h-5 w-auto"/>
								</span>
                                </div>
                            </div>
                    ))}
                </div>
            </div>
        </div>
    </section>

    {/* Seção Sobre (sem alterações) */}
    <section id="sobre" class="w-full py-16 md:py-24 bg-gradient-to-br from-green-50 via-white to-green-100">
        <div class="max-w-4xl mx-auto px-6 flex flex-col items-center text-center">
            <div class="mb-8 inline-block rounded-full p-1.5 bg-[conic-gradient(from_180deg_at_50%_50%,#EA4335_0deg,#FBBC05_90deg,#34A853_180deg,#4285F4_270deg,#EA4335_360deg)]">
                <img class="h-32 w-32 rounded-full object-cover border-4 border-white" src="/images/raimundo-coelho.png" alt="Foto de Raimundo Coelho, especialista em TI" loading="lazy" />
            </div>
            <div class="flex flex-col gap-4">
                <h2 class="text-center text-base/7 font-semibold text-green-600">Sobre Mim</h2>
                <p class="mx-auto mt-2 max-w-2xl text-center text-4xl font-semibold tracking-tight text-balance text-gray-900 sm:text-5xl">
                    Raimundo Coelho
                </p>
                <p class="text-lg font-medium text-green-900">Especialista em TI & Educador</p>
                <p class="text-lg text-gray-700 mt-2 max-w-3xl mx-auto text-balance">
                    Com mais de 20 anos de carreira dedicados à tecnologia, minha paixão é resolver problemas e capacitar pessoas. Atuo como instrutor de informática desde 1998, ensinando desde o básico de Windows e Pacote Office até desenvolvimento web e manutenção de computadores e redes. Minha missão é tornar a tecnologia acessível e funcional para todos, seja para usuários domésticos ou para empresas que buscam eficiência.
                </p>
                <ul class="mt-4 mb-2 text-left mx-auto max-w-2xl flex flex-col gap-3 text-base text-gray-800">
                    <li class="flex items-start gap-3">
                        <span class="icon-[mdi--tools] text-green-600 mt-1 flex-shrink-0"></span>
                        <span>
							<strong>Vasta Experiência em Manutenção:</strong> Especialista em diagnóstico, formatação, otimização e recuperação de dados para computadores e notebooks.
						</span>
                    </li>
                    <li class="flex items-start gap-3">
                        <span class="icon-[mdi--school-outline] text-green-600 mt-1 flex-shrink-0"></span>
                        <span>
							<strong>Educador por Vocação:</strong> Mais de 20 anos como instrutor de informática, formando centenas de alunos em cursos como Pacote Office, Design Gráfico, Desenvolvimento de Sites (HTML, CSS, JS), Servidores, Linux e Manutenção de Redes.
						</span>
                    </li>
                    <li class="flex items-start gap-3">
                        <span class="icon-[mdi--briefcase-outline] text-green-600 mt-1 flex-shrink-0"></span>
                        <span>
							<strong>Visão Comercial e Consultiva:</strong> Ampla experiência como consultor de telecomunicações para o setor governamental na Telemar/Oi e como gerente comercial, desenvolvendo propostas e cumprindo metas.
						</span>
                    </li>
                    <li class="flex items-start gap-3">
                        <span class="icon-[mdi--account-group-outline] text-green-600 mt-1 flex-shrink-0"></span>
                        <span>
							<strong>Compromisso Social:</strong> Atuo voluntariamente como Presidente do <strong>CMDCA</strong>
                            - Conselho Municial dos Direitos da Criança e Adolecente,Junto a Secretária de Ação Social, CRAS da Cidade de Divinésia/MG reforçando meu compromisso com o desenvolvimento da comunidade e a proteção dos direitos de crianças e adolescentes.
						</span>
                    </li>
                </ul>
            </div>
        </div>
    </section>

    <section id="contato" class="w-full py-16 md:py-24 bg-white">
        <div
                x-data="{
            formData: { nome: '', email: '', mensagem: '' },
            submitting: false,
            responseMessage: '',
            isError: false,
            async submitForm() {
                this.submitting = true;
                this.responseMessage = '';

                try {
                    const response = await fetch('/api/send-email', {
                        method: 'POST',
                        headers: { 'Content-Type': 'application/json' },
                        body: JSON.stringify(this.formData)
                    });

                    const result = await response.json();
                    this.responseMessage = result.message;

                    if (response.ok) {
                        this.isError = false;
                        this.formData = { nome: '', email: '', mensagem: '' }; // Limpa o formulário
                    } else {
                        this.isError = true;
                    }
                } catch (error) {
                    this.isError = true;
                    this.responseMessage = 'Não foi possível conectar ao servidor. Verifique sua conexão.';
                } finally {
                    this.submitting = false;
                }
            }
        }"
                class="max-w-3xl mx-auto px-6 flex flex-col gap-8 items-center"
        >
            <h2 class="text-center text-base/7 font-semibold text-green-600">Fale Conosco</h2>
            <p class="mx-auto mt-2 max-w-2xl text-center text-4xl font-semibold tracking-tight text-balance text-gray-900 sm:text-5xl">
                Pronto para começar?
            </p>

            <form @submit.prevent="submitForm" class="w-full flex flex-col gap-4 mt-6 max-w-xl mx-auto">
                <input x-model="formData.nome" required type="text" name="nome" placeholder="Seu nome" class="w-full border rounded-lg px-4 py-3 text-lg focus:outline-none focus:ring-2 focus:ring-offset-1 focus:ring-green-500"/>
                <input x-model="formData.email" required type="email" name="email" placeholder="Seu e-mail" class="w-full border rounded-lg px-4 py-3 text-lg focus:outline-none focus:ring-2 focus:ring-offset-1 focus:ring-green-500"/>
                <textarea x-model="formData.mensagem" required name="mensagem" rows="4" placeholder="Como podemos ajudar?" class="w-full border rounded-lg px-4 py-3 text-lg focus:outline-none focus:ring-2 focus:ring-offset-1 focus:ring-green-500"></textarea>

                <div x-show="responseMessage" x-transition :class="{ 'bg-green-100 text-green-800': !isError, 'bg-red-100 text-red-800': isError }" class="p-4 rounded-lg text-center font-medium" x-text="responseMessage"></div>

                <button type="submit" :disabled="submitting" class="bg-green-600 hover:bg-green-700 text-white font-bold px-8 py-3 rounded-lg shadow-lg transition text-lg disabled:opacity-70 disabled:cursor-not-allowed">
                    <span x-show="!submitting">Enviar Mensagem</span>
                    <span x-show="submitting">Enviando...</span>
                </button>
            </form>

            <div class="flex flex-col gap-2 items-center mt-6">
                <span class="font-semibold text-gray-700">Ou se preferir, clique direto:</span>
                <div class="flex gap-3 mt-1 flex-wrap justify-center">
                    <a href={`https://wa.me/${whatsapp}`} target="_blank" rel="noopener" class="inline-flex items-center gap-1.5 px-4 py-2 rounded-full bg-green-100 text-green-800 font-semibold hover:bg-green-200 transition"><span class="icon-[mdi--whatsapp] w-5 h-5"></span> WhatsApp</a>
                    <a href={`mailto:${emailContato}`} class="inline-flex items-center gap-1.5 px-4 py-2 rounded-full bg-blue-100 text-blue-800 font-semibold hover:bg-blue-200 transition"><span class="icon-[mdi--email] w-5 h-5"></span> E-mail</a>
                    <a href={instagramLink} target="_blank" rel="noopener" class="inline-flex items-center gap-1.5 px-4 py-2 rounded-full bg-pink-100 text-pink-700 font-semibold hover:bg-pink-200 transition"><span class="icon-[mdi--instagram] w-5 h-5"></span> Instagram</a>
                    <a href={facebookLink} target="_blank" rel="noopener" class="inline-flex items-center gap-1.5 px-4 py-2 rounded-full bg-blue-50 text-blue-800 font-semibold hover:bg-blue-100 transition"><span class="icon-[mdi--facebook] w-5 h-5"></span> Facebook</a>
                </div>
            </div>
        </div>
    </section>
</main>
<Footer/>

</body>
</html>